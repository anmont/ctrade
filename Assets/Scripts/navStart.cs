using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class navStart : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        globals.navMesh = this.gameObject;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
